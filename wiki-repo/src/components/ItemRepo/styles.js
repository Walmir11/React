import styled from "styled-components";

export const ItemContainer = styled.div`
    width: 80%;

    h3 {
        font-size: 32px;
        color: #FAFAFA;
    }

    p {
        font-size: 12px;
        color: #FAFAFA60;
        margin-top: 20px;
    }

    button.remover{
        color: #FF0000;
        margin-top: 20px;
        background: transparent;
        border: none;
        font-size: inherit;
        cursor: pointer;
        padding: 0;
        font-family: inherit;
    }

    hr{
        margin-top: 20px;
        color: #FAFAFA60;

    }
`